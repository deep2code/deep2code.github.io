StylesPath = styles
MinAlertLevel = suggestion

[*.{md,markdown}]
BasedOnStyles = ZHBlog

# 说明：
# 1. 刻意不启用 Vale 内置的拼写检查与 Microsoft/Google 风格包，
#    它们面向英文散文，对中文会大量误报。
# 2. 中文缺少空格分词，规则一律用「完整中文词/词组」做词表匹配，
#    不要依赖 \b 之类的词边界正则。
# 3. 只有 error 会让 CI 退出码非 0；warning/suggestion 不会阻断。
